## Data Types

## 1. nameric
age <- 32
print(age)
class(age)

## 2. character
my_name <- "Ploy"
my_university <- 'NIDA'
print(my_name)
print(my_university)
print(my_name);class(my_university)

## 3. logical
result <- 1 + 1 == 2
print(result)
class(result)

## 4. factor
animals <- c("Dog","Cat","Cat","Hippo")
class(animals)

animals <- factor(animals)
class(animals)

## 5. date/time
time_now <- Sys.time()
class(time_now)


money_in_my_pocket <- 150
# ชื่อตัวแปร
# หามขึ้นต้นด้วยตัวเลข
# ห้ามใช้จุด
# ภาษาอังกฤษพิมพ์เล็ก 
my_age <- 28
my_name <- "Ploy"
movie_lover <- TRUE #False
today_date <- as.Date("2024-07-02")


## check data type
class(income)
class(my_age)
class(my_name)
class(movie_lover)
class (today_date)

##change data Type 
as.numeric("100")


